# Data source switching
# One of: demo | sqlite | frappe
NEXT_PUBLIC_DATA_SOURCE=sqlite

# When using dataSource=frappe, set your API base URL (no trailing slash)
# e.g. https://api.myfrappe.instance
NEXT_PUBLIC_API_BASE_URL=

# Local SQLite database file path for the interim backend (server-side only)
# Defaults to ./.data/consultflow.db if unset
LOCAL_SQLITE_PATH=.data/consultflow.db
